# Budget Tracker Bot

Telegram-бот для управления личным бюджетом.

## Возможности

- Учет доходов и расходов по категориям
- Управление категориями (добавление своих категорий)
- Отслеживание долгов (кто кому должен)
- Получение отчетов за разные периоды
- Просмотр баланса
- Хранение всех данных в SQLite базе данных

## Установка

1. Клонируйте репозиторий или скопируйте файлы проекта

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте бота в Telegram:
   - Откройте [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте команду `/newbot`
   - Следуйте инструкциям и скопируйте полученный токен

4. Создайте файл `.env` в корне проекта:
```bash
cp .env.example .env
```

5. Откройте `.env` и вставьте токен вашего бота:
```
BOT_TOKEN=ваш_токен_от_BotFather
```

## Запуск

```bash
python main.py
```

## Структура проекта

```
budget_bot/
│
├── main.py              # Точка входа в приложение
├── config.py            # Конфигурация
├── database.py          # Работа с БД
├── handlers.py          # Обработчики команд и сообщений
├── states.py            # FSM состояния
├── keyboards.py         # Клавиатуры бота
├── utils.py             # Вспомогательные функции
├── requirements.txt     # Зависимости
├── .env.example         # Пример файла окружения
└── README.md           # Документация
```

## Использование

### Основные команды:

- `/start` - начать работу с ботом
- `/help` - справка по командам
- `/cancel` - отменить текущее действие

### Главное меню:

- **➕ Добавить доход** - записать поступление денег
- **➖ Добавить расход** - записать трату
- **💰 Баланс** - посмотреть текущий баланс
- **📊 Отчет** - получить детальный отчет за период
- **📝 Долги** - управление долгами
- **⚙️ Настройки** - настройки бота и управление категориями

### Управление категориями:

Категории хранятся в базе данных для каждого пользователя отдельно. При первом запуске бота автоматически создаются категории по умолчанию.

**Как добавить свою категорию:**
1. При добавлении дохода/расхода нажмите кнопку "➕ Добавить категорию"
2. Введите название новой категории
3. Категория будет доступна для использования

**Просмотр категорий:**
- Настройки → Управление категориями → Категории доходов/расходов

### Категории по умолчанию:

**Расходы:**
- Продукты
- Транспорт
- Развлечения
- Здоровье
- Одежда
- Связь
- Коммунальные услуги
- Другое

**Доходы:**
- Зарплата
- Фриланс
- Инвестиции
- Подарки
- Другое

## База данных

Бот использует SQLite для хранения данных. Файл базы данных `budget_bot.db` создается автоматически при первом запуске.

### Таблицы:

1. **users** - информация о пользователях
2. **transactions** - доходы и расходы
3. **debts** - учет долгов
4. **categories** - пользовательские категории (индивидуальные для каждого пользователя)

## Разработка

Бот построен на библиотеке [aiogram 3.x](https://docs.aiogram.dev/en/latest/).

### Технологии:

- Python 3.8+
- aiogram 3.4.1
- aiosqlite 0.19.0
- python-dotenv 1.0.0

## Планы развития

- [ ] Экспорт данных в Excel/CSV
- [ ] Графики и визуализация
- [ ] Повторяющиеся платежи
- [ ] Лимиты по категориям
- [ ] Мультивалютность
- [ ] Уведомления о превышении бюджета

## Автор

Создано с использованием Claude Code
